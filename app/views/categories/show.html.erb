<h2><%= @category.name %></h2><p><% if (user_signed_in?) && (can? :create, @category) %>(<%= link_to "edit", edit_category_path(@category.id) %> | <%= link_to "delete", category_path(@category.id), method: :delete, data: {confirm: "Are you sure? "}%>) <% end %></p>
<br>
<h4>List of articles :</h4>
<%#=@category.description %> 
<ul>  
	<% Article.all.find_all{|p| p.category_id==@category.id}.each do|article|%>
	<li><p><%= article.title %></p></li>
	<% end %>
</ul>
<br> 
<%= link_to "Categories list",categories_path %>
